# AGENTS.md — 今日热搜（jinri-hot-search）项目规则

> 本文件是 AI 协作规则书。每次开工前，AI 必须先读这里，按规则干活。

## 一、项目基本信息

- **项目名**：今日热搜（jinri-hot-search）
- **作者**：张光如（GitHub：709QR）
- **所属**：XingHo-VibeCoding 30 天建站训练营 · 第 1 周项目
- **仓库**：github.com/XingHo-VibeCoding/jinri-hot-search（Day 2 创建）
- **工作区**：`D:\jinri-hot-search`

## 二、项目目标

做一个「今日热搜」网站：展示当天热点榜单，随训练营进度逐步迭代（静态页 → 前后端 → 数据库）。

## 三、技术栈约定

- 前端：HTML / CSS / JavaScript（原生的优先，不引入不必要的框架）
- 后端与工具：Node.js（v22）
- 版本管理：Git，提交信息用中文或英文简短描述均可，一句话说清做了什么

## 四、AI 协作规则（最重要）

1. **AI 代办**：代码编写、文件修改、git 提交与推送由 AI 直接完成；用户只看结果、验收截图。
2. **先做后问**：常规问题 AI 自己查资料解决，不反复打扰用户；只有不可逆的删除操作、对外发布操作必须先确认。
3. **每步留痕**：每个功能完成后给出运行截图，代码和截图一一对应，方便写作业报告。
4. **不卡壳**：某一步失败先换方案，实在不行记录为待办，明天问老师，不让用户卡在一项上。
5. **不越界**：AI 不得删除 `AGENTS.md` 本文件；不得改动用户个人目录里与本项目无关的文件。
6. **当天只做当天任务**：每天只处理当天任务清单里的内容，学员没说「进入下一板块」就不越界，不提前做后面天的任务，也不回头重做已验收的旧任务。（Day 6 补录：Day 1 模板原有要求，当时漏写入文件，经核对提交历史确认后恢复）
7. **规则只增不改不删**：向本文件追加新规则时必须注明追加日期与 Day N；已生效的规则不允许修改或删除，只能以新规则覆盖说明（Day 6 追加）。
8. **推送必须验证**：git 推送完成后必须核对远程与本地一致（提交 SHA / 文件清单），不一致即停并排查，不得直接覆盖重试。（Day 6 追加，来自 Day 3/5 推送排障经验）

## 五、目录结构约定

```
jinri-hot-search/
├── AGENTS.md      # 本规则文件
├── index.html     # 网站首页（Day 2 起）
├── css/           # 样式文件
├── js/            # 脚本文件
└── docs/          # 截图与文档
```

## 六、每日打卡约定

- 每天结束按五条式格式打卡：仓库 / 首次提交 / 忽略规则 / 最大收获 / 今日未做
- 今日（Day 1）不做代码、不建仓库，只装环境 + 建工作区 + 存本文件

## 七、队友检查项（Day 6 余力加练追加）

> 第 6 条「当天只做当天任务」的队友版：任何人在本项目开工前，先过一遍这三项，全勾才算合规。

- [ ] 今天要做的内容，是否全部来自**当天**的任务清单？清单外的一律不做
- [ ] 学员明确说了「进入下一板块」吗？没说就停在当前板块，不提前开新板块
- [ ] 已验收过的旧任务，今天有没有手痒想重做/重构？有就停手，记进待办再说

## 八、任务边界细则（Day 6 深夜对标同学标准追加）

1. 当天的任务清单就是当天的一切任务范围，只做清单里写明的事。
2. 不做清单外的工作：不擅自加文件、不写「顺手加的功能」、不动已标「砍除」的方案。
3. 不提前做后面的板块；只有学员明确说「进入下一板块」或「提前做 Day X」才可动。
4. 拿不准在不在范围内，先停下问，不猜。

## 九、交互式推进（Day 6 追加）

1. 收到清单后，先给出当天执行计划再动手。
2. 每完成一步要能说清：做了什么、改了哪些文件、怎么验证。
3. 每天收尾时「停下等确认」是硬规则：全部完成后必须停，等学员确认才收工。
4. 「进入下一板块」只对当前板块有效，不等于可以连带做后面的板块。

## 十、人机分工（Day 6 追加）

1. 拍板类决策（选方案、砍功能、定优先级）由学员决定，AI 给对比和推荐，不替学员拍板；学员已授权的常规决策（每日技术选型、Bug 绕行方案）AI 可先做后报。
2. 涉及账号凭据、对外发布上线等操作必须先确认。
3. 验收要可复核：给路径、给命令、给提交号，不写「已完成」的空口结论。

## 十一、讲透环节（Day 6 追加，我是来学的）

1. 每个新概念、新命令第一次出现时，用一句话讲清它是干什么的。
2. 关键代码要讲：做了什么、为什么这么写。
3. 解释控制在一到两句，不把讲解写成教程文。

## 十二、Git 与提交（Day 6 追加）

1. commit 信息格式：`Day N｜一句话`；说明两行（改了什么 / 加了什么）。
2. 当天多次提交都挂同一天的 Day N 前缀，按板块分批提交可以，不零碎到一行一提。
3. 每次提交推送后核对远程与本地一致（见第四节第 8 条）。

## 十三、当日收尾（Day 6 追加）

1. 学员说「今天收工」后，对照原始清单逐条给完成结果：完成标准 / 状态（已完成、部分完成、未完成）/ 证据（提交号、文件、命令输出）。
2. 没做完的如实写，直接说明原因，不为「全部完成」而粉饰。
3. 全部完成后，列出改动文件清单、说明各属于哪天的任务、提醒学员该交的作业和该在群里回什么。

## 十四、出问题的时候（Day 6 追加）

1. 出问题先自己回看定位（git log、快照历史）；禁止 `git reset --hard` 和强制推送。
2. 同一个问题反复失败 3 次，停下来报告原因，不闷头重试。
3. 降级优先保交付：真站截图失败走本地仿制、某条路走不通换路径，记录进当日日志，不让学员卡住。
