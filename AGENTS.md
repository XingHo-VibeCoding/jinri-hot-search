# AGENTS.md — 今日热搜（jinri-hot-search）项目规则

> 本文件是 AI 协作规则书。每次开工前，AI 必须先读这里，按规则干活。

## 一、项目基本信息

- **项目名**：今日热搜（jinri-hot-search）
- **作者**：张光如（GitHub：709QR）
- **所属**：XingHo-VibeCoding 30 天建站训练营 · 第 1 周项目
- **仓库**：github.com/XingHo-VibeCoding/jinri-hot-search（Day 2 创建）
- **工作区**：`D:\jinri-hot-search`

## 二、项目目标

做一个「今日热搜」网站：展示当天热点榜单，随训练营进度逐步迭代（静态页 → 前后端 → 数据库）。

## 三、技术栈约定

- 前端：HTML / CSS / JavaScript（原生的优先，不引入不必要的框架）
- 后端与工具：Node.js（v22）
- 版本管理：Git，提交信息用中文或英文简短描述均可，一句话说清做了什么

## 四、AI 协作规则（最重要）

1. **AI 代办**：代码编写、文件修改、git 提交与推送由 AI 直接完成；用户只看结果、验收截图。
2. **先做后问**：常规问题 AI 自己查资料解决，不反复打扰用户；只有不可逆的删除操作、对外发布操作必须先确认。
3. **每步留痕**：每个功能完成后给出运行截图，代码和截图一一对应，方便写作业报告。
4. **不卡壳**：某一步失败先换方案，实在不行记录为待办，明天问老师，不让用户卡在一项上。
5. **不越界**：AI 不得删除 `AGENTS.md` 本文件；不得改动用户个人目录里与本项目无关的文件。
6. **当天只做当天任务**：每天只处理当天任务清单里的内容，学员没说「进入下一板块」就不越界，不提前做后面天的任务，也不回头重做已验收的旧任务。（Day 6 补录：Day 1 模板原有要求，当时漏写入文件，经核对提交历史确认后恢复）
7. **规则只增不改不删**：向本文件追加新规则时必须注明追加日期与 Day N；已生效的规则不允许修改或删除，只能以新规则覆盖说明（Day 6 追加）。
8. **推送必须验证**：git 推送完成后必须核对远程与本地一致（提交 SHA / 文件清单），不一致即停并排查，不得直接覆盖重试。（Day 6 追加，来自 Day 3/5 推送排障经验）

## 五、目录结构约定

```
jinri-hot-search/
├── AGENTS.md      # 本规则文件
├── index.html     # 网站首页（Day 2 起）
├── css/           # 样式文件
├── js/            # 脚本文件
└── docs/          # 截图与文档
```

## 六、每日打卡约定

- 每天结束按五条式格式打卡：仓库 / 首次提交 / 忽略规则 / 最大收获 / 今日未做
- 今日（Day 1）不做代码、不建仓库，只装环境 + 建工作区 + 存本文件
