# PRD.md — 今日热搜 产品需求文档

> Day 4 产出 · 作者：张光如（709QR） · 2026-09-21 · 版本 v1.0
> 上游文档：[research.md](research.md)（需求研究）· 本文档只回答「做什么」，不回答「怎么做」

## 1. 产品概述

**一句话定位**：每天最热的事，一页看完——极简今日热搜展示页，打开就能看到当天热点榜单，不登录、不跳转、不催下载。

**要解决的问题**：热点散落在多个平台，看全要装一堆 App、忍受登录和广告。本产品把当天最有价值的热点聚合成一个秒开的单页。

## 2. 目标用户与场景

| 角色 | 场景 | 核心诉求 |
|---|---|---|
| 学生（主要用户） | 早上刷一遍、课间瞄一眼 | 10 秒内看完今天最热的事 |
| 同学/朋友 | 听说后好奇来访问 | 不注册就能看，页面秒开 |
| 老师（验收人） | 作业评审 | 流程完整：研究→PRD→MVP→部署 |

**用户故事（示例）**：作为一名学生，我想在课间打开一个网页就能看到今天的热点榜单，这样我不装任何 App、不登录任何账号也能 10 秒钟掌握今天最热的事。

## 3. 页面结构（仅一页）

```
┌─────────────────────────────┐
│  ① 标题区：今日热搜 + 当天日期  │
├─────────────────────────────┤
│  ② 工具区：「换一批」按钮        │
├─────────────────────────────┤
│  ③ 榜单区（核心，占满首屏）      │
│     排名 | 标题 | 热度值 | 来源  │
│     ...共 20 条...            │
└─────────────────────────────┘
```

- 没有详情页、没有二级页面；点击条目只做**高亮**（标记已看），不发生跳转
- 页面底部一行小字：数据更新时间 + 项目署名

## 4. 功能需求

| 编号 | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F1 | 榜单展示 | P0 | 展示 20 条热点，每条含：排名、标题、热度值、来源标签（4 个字段缺一不可） |
| F2 | 换一批 | P0 | 点击后从数据池随机换 20 条展示；高亮状态跟随条目本身（按标题匹配，条目再次出现仍保持高亮） |
| F3 | 条目高亮 | P1 | 点击条目切换高亮/取消，用于标记「看过了」 |
| F4 | 日期显示 | P1 | 标题区显示数据对应的日期（来自数据文件的 date 字段） |

## 5. 数据结构（静态 JSON 文件）

```json
{
  "date": "2026-09-22",
  "items": [
    { "rank": 1, "title": "热点标题", "heat": 986543, "source": "微博" }
  ]
}
```

- 数据池不少于 **40 条**（保证「换一批」有得换），来源覆盖微博/百度/知乎
- 文件放本地 `data/` 目录，页面用 fetch 读取；Day 23 数据库化时仅替换数据来源，字段结构不变

## 6. 明确不做（本期非目标）

与 research.md「本期不做清单」保持一致，PRD 层面再次锁定：

1. 多平台实时爬虫（数据每日手工更新静态 JSON）
2. 用户注册/登录
3. 评论、点赞等互动功能
4. 关键词搜索
5. 深色模式（留作 Day 7 后打磨）
6. 移动端 App（网页响应式即可）
7. 自动实时更新（无推送、无长连接）

## 7. 验收标准（Day 7 逐条打勾）

| # | 验收项 | 通过标准 |
|---|---|---|
| A1 | 榜单完整展示 | 打开页面即见 20 条榜单，每条含排名、标题、热度值、来源标签 4 字段 |
| A2 | 首屏速度 | 本地打开 3 秒内完整渲染（目标 1 秒内） |
| A3 | 换一批可用 | 点击「换一批」后榜单内容变化，无控制台报错，连点 5 次不崩 |
| A4 | 高亮可用 | 点击任意条目出现可见的高亮样式，再点取消 |
| A5 | 移动端不破 | 375px 宽度（iPhone SE）下无横向滚动条，榜单不重叠 |
| A6 | 克制验收 | 页面上无登录入口、无广告位、无外跳链接 |
| A7 | 数据结构 | data JSON 含 date + items，items ≥ 40 条，字段与第 5 节一致 |
| A8 | 入库规范 | 代码在 GitHub main 分支，提交信息以「Day N｜」开头 |
| A9 | 日期正确 | 标题区显示的日期与数据文件 date 字段一致 |

## 8. 里程碑

| 时间 | 交付 |
|---|---|
| Day 5 | 技术选型（本 PRD 不涉及） |
| Day 5~6 | 按 F1~F4 开发 |
| Day 7 | 按 A1~A8 验收，MVP 上线 |
