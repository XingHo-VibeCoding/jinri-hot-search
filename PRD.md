# PRD.md — 今日热搜 产品需求文档

> Day 4 产出 · 作者：张光如（709QR） · 2026-09-21 · 版本 v1.0
> 上游文档：[research.md](research.md)（需求研究）· 本文档只回答「做什么」，不回答「怎么做」

## 1. 产品概述

**一句话定位**：每天最热的事，一页看完——极简今日热搜展示页，打开就能看到当天热点榜单，不登录、不跳转、不催下载。

**要解决的问题**：热点散落在多个平台，看全要装一堆 App、忍受登录和广告。本产品把当天最有价值的热点聚合成一个秒开的单页。

## 2. 目标用户与场景

| 角色 | 场景 | 核心诉求 |
|---|---|---|
| 学生（主要用户） | 早上刷一遍、课间瞄一眼 | 10 秒内看完今天最热的事 |
| 同学/朋友 | 听说后好奇来访问 | 不注册就能看，页面秒开 |
| 老师（验收人） | 作业评审 | 流程完整：研究→PRD→MVP→部署 |

**用户故事（示例）**：作为一名学生，我想在课间打开一个网页就能看到今天的热点榜单，这样我不装任何 App、不登录任何账号也能 10 秒钟掌握今天最热的事。

## 3. 页面结构（仅一页）

```
┌─────────────────────────────┐
│  ① 标题区：今日热搜 + 当天日期  │
├─────────────────────────────┤
│  ② 工具区：「换一批」按钮        │
├─────────────────────────────┤
│  ③ 榜单区（核心，占满首屏）      │
│     排名 | 标题 | 热度值 | 来源  │
│     ...共 20 条...            │
└─────────────────────────────┘
```

- 没有详情页、没有二级页面；点击条目只做**高亮**（标记已看），不发生跳转
- 页面底部一行小字：数据更新时间 + 项目署名

## 4. 功能需求

| 编号 | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F1 | 榜单展示 | P0 | 展示 20 条热点，每条含：排名、标题、热度值、来源标签（4 个字段缺一不可） |
| F2 | 换一批 | P0 | 点击后从数据池随机换 20 条展示；高亮状态跟随条目本身（按标题匹配，条目再次出现仍保持高亮） |
| F3 | 条目高亮 | P1 | 点击条目切换高亮/取消，用于标记「看过了」 |
| F4 | 日期显示 | P1 | 标题区显示数据对应的日期（来自数据文件的 date 字段） |

## 5. 数据结构（静态 JSON 文件）

```json
{
  "date": "2026-09-22",
  "items": [
    { "rank": 1, "title": "热点标题", "heat": 986543, "source": "微博" }
  ]
}
```

- 数据池不少于 **40 条**（保证「换一批」有得换），来源覆盖微博/百度/知乎
- 文件放本地 `data/` 目录，页面用 fetch 读取；Day 23 数据库化时仅替换数据来源，字段结构不变

## 6. 边界（从来就没打算做）

与 research.md 结论一致，以下是产品边界——不是「砍掉」，是出生时就不在范围内：

1. 用户注册/登录：纯浏览场景，没有需要账号才能完成的动作
2. 评论、点赞等互动：没有用户体系就没有互动主体
3. 关键词搜索：单页只有 20~30 条，肉眼可扫完，搜索是伪需求
4. 移动端 App：网页响应式即可覆盖手机浏览器
5. 自动实时更新：每日更新一次已满足「今天最热」，无推送、无长连接

## 6.5 砍掉的功能与原因（曾经想要，明确砍掉）

与第 6 节的区别：本节是「讨论过、动过念头、最终砍掉」的东西——砍的过程本身有决策价值，所以留档。

**6.5.1 多平台实时爬虫**
- 曾经的想法：抓取微博/百度/知乎实时热搜，数据越全越有价值
- 砍掉的原因（两条）：① research.md 已证明 tophub 把「聚合」做透了，拼数据量没有胜算；② 实时爬虫需要后端定时任务 + 反爬对抗，超出第 1 周能力
- 替代方案：每日手工更新静态 JSON——保住「今天最热」的核心价值，把精力全部投到「秒开、无门槛」的体验上

**6.5.2 深色模式**
- 曾经的想法：加上显得专业，成本也不高
- 砍掉的原因：不解决「10 秒看完」的核心诉求，且样式返工风险会拖慢 Day 5~6 开发
- 替代方案：留作 Day 7 验收后的打磨项，不进本期范围

## 7. 验收标准（Day 7 逐条打勾）

> **测试条件（A2 计时前提）**：最新版 Chrome、常规家用宽带、首次访问（清缓存）条件下计时；**本地 file:// 或 localhost 打开不计入**——避免把开发环境的速度当成结论。移动端用浏览器开发者工具 375px 模拟，Day 7 有条件再用真机复核。

| # | 对应功能 | 验收项 | 通过标准 |
|---|---|---|---|
| A1 | F1 | 榜单完整展示 | 打开页面即见 20 条榜单，每条含排名、标题、热度值、来源标签 4 字段 |
| A2 | F1 | 首屏速度 | 在测试条件下 3 秒内完整渲染（目标 1 秒内） |
| A3 | F2 | 换一批可用 | 点击「换一批」后榜单内容变化，无控制台报错，连点 5 次不崩 |
| A4 | F3 | 高亮可用 | 点击任意条目出现可见的高亮样式，再点取消 |
| A5 | 全部 | 移动端不破 | 375px 宽度（iPhone SE 模拟）下无横向滚动条，榜单不重叠 |
| A6 | 全部 | 克制验收 | 页面上无登录入口、无广告位、无外跳链接 |
| A7 | 第 5 节 | 数据结构 | data JSON 含 date + items，items ≥ 40 条，字段与第 5 节一致 |
| A8 | 流程 | 入库规范 | 代码在 GitHub main 分支，提交信息以「Day N｜」开头 |
| A9 | F4 | 日期正确 | 标题区显示的日期与数据文件 date 字段一致 |
| A10 | 第 6 节 | 数据诚实 | 页面标注「演示数据」及数据更新日期，不冒充当日实时数据 |

**「必须修改」判定线**：Day 7 验收时，A1~A10 任何一条当场做不到，即属「必须修改」级问题；做到但有瑕疵的，属「建议修改」。开发期间自检按同一判定线执行。

## 8. 里程碑

| 时间 | 交付 |
|---|---|
| Day 5 | 技术选型（本 PRD 不涉及） |
| Day 5~6 | 按 F1~F4 开发 |
| Day 7 | 按 A1~A10 验收，MVP 上线 |
